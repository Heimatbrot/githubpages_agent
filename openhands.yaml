model:
  provider: openai
  model: gpt-4.1-mini

permissions:
  filesystem:
    read: true
    write: true
  shell: true

limits:
  max_steps: 100
  max_runtime_seconds: 1800

tasks:
  - file: task.txt